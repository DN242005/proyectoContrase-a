version: '3.8'
services:
  db:
    image: postgres:15
    container_name: uv_postgres_db
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: admin123
      POSTGRES_DB: uv_recovery_db
    ports:
      - "5432:5432"